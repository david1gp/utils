export function intFloor(n: number) {
  return Math.floor(n)
}

export function intFloor5(n: number) {
  return Math.floor(n / 5) * 5
}

export function intFloor10(n: number) {
  return Math.floor(n / 10) * 10
}
