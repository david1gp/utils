export function intRound(n: number) {
  return Math.round(n)
}
export function intRound5(n: number) {
  return Math.round(n / 5) * 5
}
export function intRound10(n: number) {
  return Math.round(n / 10) * 10
}
